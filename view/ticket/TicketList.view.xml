<mvc:View
    controllerName="sap.ui.demo.nav.controller.ticket.TicketList"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc">
    
    <Page id="ticketListPage" title="{i18n>TicketList}"
		showNavButton="true"
		navButtonPress="onNavBack"
		class="sapUiResponsiveContentPadding">
		<subHeader>
            <Toolbar>
                <SearchField
                    liveChange="onFilterTickets"/>
            </Toolbar>
        </subHeader>
        <ScrollContainer vertical="true" getParent="true">
            <layoutData>
                <FlexItemData growFactor="1"/>
            </layoutData>
            <List id="ticketsList"
                items="{
                    path: '/Tickets',
                    filters: [{
                        path: 'description_status',
                        operator: 'NE',
                        value1: 'Closed'
                    }]
                }">
                <items> 
                    <ObjectListItem
                        title="{ticketTitle} - {description_functionalArea}"
                        type="Active"
                        press="onTicketListItemPress"
                        number="{
                            parts:[{path:'description_priority'}]
                        }">
                        <firstStatus>
                            <ObjectStatus
                                text="{description_status}"
                            />
                        </firstStatus>
                        <attributes>
            				<ObjectAttribute text="Ticket creator:  {firstName_creator} {lastName_creator}" />
            				<ObjectAttribute text="Ticket assignee: {firstName_assignee} {lastName_assignee}" />
                        </attributes>
                    </ObjectListItem>
                </items>
            </List>
        </ScrollContainer>
    </Page>
</mvc:View>