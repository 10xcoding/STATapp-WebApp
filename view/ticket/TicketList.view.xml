<mvc:View
    id="ticketListView"
    controllerName="statapp.controller.ticket.TicketList"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc">
    
    <Page id="ticketListPage" title="{i18n>TicketList.Title}"
		showNavButton="true"
		navButtonPress="onNavBack"
		class="sapUiResponsiveContentPadding"
		rerender="true">
		<subHeader>
            <Toolbar>
                <SearchField
                    liveChange="onFilterTickets"/>
            </Toolbar>
        </subHeader>
        <ScrollContainer vertical="true" getParent="true">
            <layoutData>
                <FlexItemData growFactor="1"/>
            </layoutData>
            <List
                id="ticketsList"
                items="{
                    path: '/Tickets',
                    filters: [{
                        path: 'description_status',
                        operator: 'NE',
                        value1: 'Closed'
                    }],
                    sort: 'dateCreated'
                }">
                <items> 
                    <ObjectListItem
                        title="{ticketTitle} - {description_functionalArea}"
                        type="Active"
                        press="onTicketListItemPress"
                        number="{
                            parts:[{path:'description_priority'}]
                        }">
                        <firstStatus>
                            <ObjectStatus
                                text="{description_status}"
                            />
                        </firstStatus>
                        <attributes>
                            <ObjectAttribute text="{i18n>Ticket.Creator}: {firstName_creator} {lastName_creator}" />
                            <ObjectAttribute text="{i18n>Ticket.Assignee}: {firstName_assignee} {lastName_assignee}" />
                        </attributes>
                    </ObjectListItem>
                </items>
            </List>
        </ScrollContainer>
        <footer>
			<OverflowToolbar id="TicketListFooter">
                <ToolbarSpacer/>
                <Button id="newTicketButton" text="{i18n>TicketList.NewTicketButton}" press="onNavToNewTicket" icon="sap-icon://add"/>
            </OverflowToolbar>
        </footer>
    </Page>
</mvc:View>