<mvc:View
    id="ticketListView"
    controllerName="statapp.controller.ticket.TicketList"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc">
    
    <Page id="ticketListPage" title="{i18n>TicketList.Title}"
		showNavButton="true"
		navButtonPress="onNavBack"
		class="sapUiResponsiveContentPadding" >
		<subHeader>
            <Toolbar>
                <SearchField
                    liveChange="onFilterTickets"/>
            </Toolbar>
        </subHeader>
        <ScrollContainer vertical="true" getParent="true">
            <layoutData>
                <FlexItemData growFactor="1"/>
            </layoutData>
            <List
                id="ticketsList"
                items="{
                    path: '/Tickets',
                    filters: [{
                        path: 'ticketStatus_description',
                        operator: 'NE',
                        value1: 'Closed'
                    }],
                    sort: 'dateCreated'
                }">
                <items> 
                    <ObjectListItem
                        title="{ticketTitle} - {functionalArea_description}"
                        type="Active"
                        press="onTicketListItemPress"
                        number="{
                            parts:[{
                                path:'ticketPriority_description'
                            }]
                        }">
                        <firstStatus>
                            <ObjectStatus
                                text="{ticketStatus_description}"
                            />
                        </firstStatus>
                        <attributes>
                            <ObjectAttribute text="{i18n>Ticket.Creator}: {creator_firstName} {creator_lastName}" />
                            <ObjectAttribute text="{i18n>Ticket.Assignee}: {assignee_firstName} {assignee_lastName}" />
                        </attributes>
                    </ObjectListItem>
                </items>
            </List>
        </ScrollContainer>
        <footer>
			<OverflowToolbar id="TicketListFooter">
                <ToolbarSpacer/>
                <Button id="newTicketButton" text="{i18n>TicketList.NewTicketButton}" press="onNavToNewTicket" icon="sap-icon://add"/>
            </OverflowToolbar>
        </footer>
    </Page>
</mvc:View>