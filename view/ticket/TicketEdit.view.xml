<mvc:View
	controllerName="sap.ui.demo.nav.controller.ticket.TicketEdit"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:f="sap.ui.layout.form"
	xmlns:core="sap.ui.core"
	busyIndicatorDelay="0">
	<Page
		id="ticketEditPage"
		title="{i18n>TicketEdit.Title} {ticketId}"
		showNavButton="true"
		navButtonPress="onNavBack"
		class="sapUiResponsiveContentPadding">
		<Panel
			id="editTicketPanel"
			width="auto"
			class="sapUiResponsiveMargin sapUiNoContentPadding">
			<headerToolbar>
				<Toolbar>
					<Title text="{i18n>TicketEdit.ToolbarTitle}" level="H2"/>
					<ToolbarSpacer />
				</Toolbar>
			</headerToolbar>
			<content>
				<f:SimpleForm
					minWidth="1024"
					layout="ResponsiveGridLayout"
					labelSpanL="3" labelSpanM="3" emptySpanL="4" emptySpanM="4" columnsL="1" columnsM="1">
					<f:content>
						<Label text="{i18n>Ticket.Creator}" />
						<Input value="{firstName_creator} {lastName_creator}" editable="false" />
						<Label text="{i18n>Ticket.DateCreated}" />
						<Input value="{
                            path: 'dateCreated',
                            type: 'sap.ui.model.type.Date',
                            formatOptions: {
                                style: 'medium'
                            }
                        }" editable="false" />
						<Label text="{i18n>Ticket.DateStarted}" />
						<Input value="{
                            path: 'dateStarted',
                            type: 'sap.ui.model.type.Date',
                            formatOptions: {
                                style: 'medium'
                            }
                        }" editable="false" />
						<Label text="{i18n>Ticket.DateClosed}" />
						<Input value="{
                            path: 'dateClosed',
                            type: 'sap.ui.model.type.Date',
                            formatOptions: {
                                style: 'medium'
                            }
                        }" editable="false" />
					</f:content>
				</f:SimpleForm>
				<f:SimpleForm
					minWidth="1024"
					editable="true"
					layout="ResponsiveGridLayout"
					labelSpanL="3" labelSpanM="3" emptySpanL="4" emptySpanM="4" columnsL="1" columnsM="1">
					<f:content>
						<Label text="{i18n>Ticket.TicketTitle}" required="true" />
						<Input value="{ticketTitle}" id="editTicketTitle" />
						
						<Label text="{i18n>Ticket.TicketDescription}" required="true" />
						<TextArea value="{ticketDescription}" id="editTicketDescription" />
						
						<Label text="{i18n>Ticket.FunctionalArea}" required="true" />
						<Select
                    		id="funcAreaSelect"
                    		items="{
                    			path: 'FuncAreaModel>/FuncAreas'
                    		}">
                    		<items>
                    			<core:Item
                    			    selectedKey="{functionalArea_value}"
                    			    key="{FuncAreaModel>value}"
                    			    text="{FuncAreaModel>description}" />
                    		</items>
                    	</Select>
						
						<Label text="{i18n>Ticket.Assignee}" required="true" />
						<Input
            				id="assigneeInput"
            				type="Text"
            				placeholder="Enter Name ..."
            				showSuggestion="true"
            				suggestionItems="{
                    			path: 'UserModel>/Users'
                    		}">
            				<suggestionItems>
            					<core:ListItem
            					    selectedKey="{assignee_userId}"
                    			    key="{UserModel>userId}"
            					    text="{UserModel>firstName} {UserModel>lastName}"
            					    additionalText="{UserModel>userId}" />
            				</suggestionItems>
            			</Input>
						
						<Label text="{i18n>Ticket.TicketStatus}" required="true" />
						<Select
                    		id="ticketStatusSelect"
                    		items="{
                    			path: 'ticketStatusModel>/TicketStatuses'
                    		}">
                    		<items>
                    			<core:Item
                    			    selectedKey="{ticketStatus_value}"
                    			    key="{ticketStatusModel>value}"
                    			    text="{ticketStatusModel>description}" />
                    		</items>
                    	</Select>
						
						<Label text="{i18n>Ticket.TicketPriority}" required="true" />
						<Select
                    		id="ticketPrioritySelect"
                    		items="{
                    			path: 'TicketPriorityModel>/TicketPriorities'
                    		}">
                    		<items>
                    			<core:Item
                    			    selectedKey="ticketPriority_value"
                    			    key="{TicketPriorityModel>value}"
                    			    text="{TicketPriorityModel>description}" />
                    		</items>
                    	</Select>
					</f:content>
				</f:SimpleForm>
			</content>
		</Panel>
		<footer>
			<OverflowToolbar id="TicketListFooter">
			    <ToolbarSpacer/>
			    <Button id="newTicketButton" text="{i18n>TicketList.NewTicketButton}" press="onNavToNewTicket" icon="sap-icon://add"/>
		    </OverflowToolbar>
        </footer>
		<footer>
			<OverflowToolbar id="TicketEditFooter">
			    <ToolbarSpacer/>
			    <Button 
    			    type="Reject"
    			    text="{i18n>Cancel}"
    			    icon="sap-icon://cancel"
    			    press="onPressCancel"
    			/>
    			<ToolbarSpacer width="0.1em"/>
    			<Button
    			    type="Accept"
    			    text="{i18n>Save}"
    			    icon="sap-icon://save"
    			    press="onPressSave"
    			/>
		    </OverflowToolbar>
		</footer>
	</Page>
</mvc:View>